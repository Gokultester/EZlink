	$ESC (
	***********JOURNAL RECORD***********
	$LF
	$TERMLOC
	$LF
	$DATE[/] $SPACE $TIME[:] $SPACE OBLN $TERMID
	$LF
	TX# $SPACE $ATMSEQNO $SPACE RCODE# $SPACE 0  $RESPCODE
	$LF
	CARD $SPACE $CHN $SPACE AUTH# $SPACE $AUTHNUM
	$LF
	A/C $SPACE $FROM_ACCT
	$LF
	TXN $SPACE $TRANSACTION
	$LF
